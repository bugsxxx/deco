<%= form_for(@sidekiqexemplo) do |f| %>
  <% if @sidekiqexemplo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sidekiqexemplo.errors.count, "error") %> prohibited this sidekiqexemplo from being saved:</h2>

      <ul>
      <% @sidekiqexemplo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo %><br>
    <%= f.text_field :tipo %>
  </div>
  <div class="field">
    <%= f.label :codigo %><br>
    <%= f.text_area :codigo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
